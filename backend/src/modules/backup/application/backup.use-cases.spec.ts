import { BackupPort } from "./backup.port";import { CreateBackupUseCase,ListBackupsUseCase,RestoreBackupUseCase } from "./backup.use-cases";const job={id:"b1",status:"SUCCEEDED",filePath:"x",createdAt:new Date()};class Port implements BackupPort{restored=false;create(){return Promise.resolve(job as any);}list(){return Promise.resolve([job] as any);}restore(){this.restored=true;return Promise.resolve({...job,status:"SUCCEEDED"} as any);}}describe("Backup use cases",()=>{it("creates and lists backups",async()=>{const p=new Port();expect((await new CreateBackupUseCase(p).execute("admin")).status).toBe("SUCCEEDED");expect(await new ListBackupsUseCase(p).execute()).toHaveLength(1);});it("requires explicit restore confirmation",async()=>{const p=new Port(),uc=new RestoreBackupUseCase(p);await expect(uc.execute({actorId:"admin",backupId:"b1",confirmation:"no"})).rejects.toThrow("RESTORE");await uc.execute({actorId:"admin",backupId:"b1",confirmation:"RESTORE"});expect(p.restored).toBe(true);});});
