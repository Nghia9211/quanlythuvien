import { ReportingQueryPort } from "./reporting-query.port";export class GetOperationalReportUseCase{constructor(private query:ReportingQueryPort){}async execute(input:{from:string;to:string;branchId?:string}){const from=new Date(input.from),to=new Date(input.to);if(Number.isNaN(from.getTime())||Number.isNaN(to.getTime())||from>to)throw new Error("Invalid report date range");to.setUTCHours(23,59,59,999);return this.query.getOperationalSummary({from,to,branchId:input.branchId});}}
